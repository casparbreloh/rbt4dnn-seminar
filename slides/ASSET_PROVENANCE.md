# Slide asset provenance

The HTML presentation is authored for this seminar project and licensed under CC BY 4.0,
subject to the repository-wide exclusions in [`../NOTICE.md`](../NOTICE.md). Most visuals are
inline HTML/CSS/SVG reconstructions from committed tabular results, so the presentation does not
need copied figures from the paper or course material.

## Published assets

| File | Origin | Purpose | License note |
| --- | --- | --- | --- |
| `assets/mnist-shared-generator-samples.png` | Generated by this project's conditional-VAE extension (`experiments/mnist-shared-generator/`) | Illustrates the easy-domain shared-generator baseline on slide 22 | Original experiment output, CC BY 4.0 under `NOTICE.md`; model training used the locally restored upstream corpus, so downstream users should still review upstream and dataset terms. |
| `assets/celeba-shared-generator-samples.png` | Generated by this project's conditional-VAE extension (`experiments/celeba-shared-generator/`) | Supports the harder natural-image baseline retained in the experiment artifact | Original experiment output, CC BY 4.0 under `NOTICE.md`; model training used the locally restored upstream corpus and CelebA-family data may carry additional terms. |

SHA-256 checksums:

- `mnist-shared-generator-samples.png`: `0705b90ccb1b70a12bec0dc2732d4dc5be857a05044b0e26316273f1ccfccc29`
- `celeba-shared-generator-samples.png`: `bfe9effe43fe3390ce465749406100021492e0030437f59ab7c00466b492be97`

Both files are exact copies of the committed experiment outputs. They are included because they
are independently generated results of this repository's extension experiments, not copied
paper figures or copied dataset images.

## Public artifact samples in the live deck

The live presentation hot-links a small, cited sample directly from the authors' public
artifact at the pinned commit recorded in `data/manifest.json`. The images are not copied
into this repository or the GitHub Pages artifact because the upstream repository does not
declare a redistribution license. Paper tables and diagrams are redrawn as native HTML for
legibility rather than republished as crops.

`rbt4dnn-presentation.pdf` is a publication-safe export. The export script renders the deck,
replaces the upstream sample regions with source-linked placeholders, and assembles a flattened
PDF that retains only visual assets authored by this seminar project. The export is reproducible
with `scripts/export_presentation_pdf.sh`.

## Deliberately excluded material

- TUM marks and course-slide images: not republished.
- Full paper pages and paper imagery: explanatory figures and evidence tables are redrawn as
  native HTML where needed.
- Images copied from the upstream RBT4DNN artifact: not republished; reviewers can restore the
  pinned corpus through `scripts/fetch_data.py`.
- Dataset photographs and examples: not copied into the deck.

## Data-backed inline visuals

The deck's replication, validity, cost, shared-generator, and LLM-audit graphics are authored as
inline HTML/CSS/SVG. Values come from committed CSV files under `experiments/`; the slide captions
identify the relevant experiment and limitations. See the final slide for the original paper and
artifact links.
